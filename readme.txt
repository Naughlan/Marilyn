goto to google and search how to create a react app
open git-bash and enter the commands show at the site to the terminal
create app on both client and server site

create components, screens and redux folders in src folder
    install dependencies : chakra-ui...
After, edit the App.js and import downloaded dependencies (incl. Navbar)
create a Navbar component
    use the abbr. shortcut (rafce) installed to create a template structure